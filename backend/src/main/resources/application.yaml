spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/mechanicflow
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
