spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgres://localhost:5432/mechanicflow
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
